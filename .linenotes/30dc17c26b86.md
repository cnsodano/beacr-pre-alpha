#! _RETURN make generalizable for extensibility to new beacon schema; for now hard-coded for NIST
Here allow some option to check if the pkg is being used in "streaming" mode i.e. continually grab new pulses, in which case part of validation would be checking 
    - if the new pulse is different from the previous one used (not the last pulse issued, but the previous pulse queried by the pkg; if queried too frequently (faster than the pulse freshness period) then they will not be different and you will need to wait for a fresh pulse)

Also in future add checks of timing:
    - was the pulse issues the correct amount of time after the last pulse and the correct amount of time before the next pulse?
