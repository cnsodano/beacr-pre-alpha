
            # if (delta["year"] > 0) {
            #   for (year in seq(abs(delta["year"]))) {
            #     curr_pulse_record_of_previous_year_output_value = self$get_prev_value_from_pulse(
            #       curr_pulse,
            #       type_ = "year"
            #     )
            #     prev_year_time = self$convert_to_unix_milliseconds(
            #       curr_time - lubridate::years(1)
            #     )
            #     actual_pulse_of_prev_year = self$get_pulse_by_timestamp(
            #       prev_year_time
            #     )
            #     #! _RETURN in future schema there may be a distinction made
            #     #! that you can supply a chain index as well as timestamp

            #     actual_pulse_of_prev_year_output = actual_pulse_of_prev_year$outputValue
            #     stopifnot(
            #       actual_pulse_of_prev_year_output ==
            #         curr_pulse_record_of_previous_year_output_value
            #     )
            #     curr_pulse = actual_pulse_of_prev_year
            #   }
            #   for (month in seq(abs(delta["month"]))) {
            #     curr_pulse_record_of_previous_month_output_value = self$get_prev_value_from_pulse(
            #       curr_pulse,
            #       type_ = "month"
            #     )
            #     prev_month_time = self$convert_to_unix_milliseconds(
            #       curr_time - lubridate::months(1)
            #     )
            #     actual_pulse_of_prev_month = pulse_time(
            #       prev_month_time
            #     )
            #     #! _RETURN in future schema there may be a distinction made
            #     #! that you can supply a chain index as well as timestamp

            #     actual_pulse_of_prev_month_output = actual_pulse_of_prev_month$outputValue
            #     stopifnot(
            #       actual_pulse_of_prev_month_output ==
            #         curr_pulse_record_of_previous_month_output_value
            #     )
            #     curr_pulse = actual_pulse_of_prev_month
            #   }
            #   for (day in seq(abs(delta["day"]))) {
            #     curr_pulse_record_of_previous_day_output_value = self$get_prev_value_from_pulse(
            #       curr_pulse,
            #       type_ = "day"
            #     )
            #     prev_day_time = self$convert_to_unix_milliseconds(
            #       curr_time - lubridate::days(1)
            #     )
            #     actual_pulse_of_prev_day = pulse_time(
            #       prev_day_time
            #     )
            #     #! _RETURN in future schema there may be a distinction made
            #     #! that you can supply a chain index as well as timestamp

            #     actual_pulse_of_prev_day_output = actual_pulse_of_prev_day$outputValue
            #     stopifnot(
            #       actual_pulse_of_prev_day_output ==
            #         curr_pulse_record_of_previous_day_output_value
            #     )
            #     curr_pulse = actual_pulse_of_prev_day
            #   }
            #   for (hour in seq(abs(delta["hour"]))) {
            #     curr_pulse_record_of_previous_hour_output_value = self$get_prev_value_from_pulse(
            #       curr_pulse,
            #       type_ = "hour"
            #     )
            #     prev_hour_time = self$convert_to_unix_milliseconds(
            #       curr_time - lubridate::hours(1)
            #     )
            #     actual_pulse_of_prev_hour = pulse_time(
            #       prev_hour_time
            #     )
            #     #! _RETURN in future schema there may be a distinction made
            #     #! that you can supply a chain index as well as timestamp

            #     actual_pulse_of_prev_hour_output = actual_pulse_of_prev_hour$outputValue
            #     stopifnot(
            #       actual_pulse_of_prev_hour_output ==
            #         curr_pulse_record_of_previous_hour_output_value
            #     )
            #     curr_pulse = actual_pulse_of_prev_hour
            #   }