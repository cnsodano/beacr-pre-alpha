Notes on precommit:

#! _RETURN option to read from file not just pass as arg. Option to offer links, etc. If passing in, send warning that the point is to have it already published, not just typing it in on the day of...
#! If not already in the text, concatenate info about what pulse you will request to 'pre-commit' to whatever the seed generated is BEFORE that is available
#! _RETURN append to the text "generated by ---version, etc...on...timestamp..."
#! _RETURN add options for signing with public key AND options for encrypting for later release


