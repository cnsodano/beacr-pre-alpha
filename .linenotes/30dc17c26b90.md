#' #!_RETURN
#' input unique file id (in future guid of the registration)
#' osf_retrieve_file() allows the 5char code in the file url which is nice, but in future would like to verify that its a registration not a normal proj file
#' during first call, throw error if timestamp of registration is later than timestamp/pulse id->fetch-> timestamp of pulse (give buffer of 1 hour for gaps?)
#' fileid>getfile>extra>hashes>sha256>pull and use as concat for seed
#' during replay, verify that timestamp
#'  during replay, pull that same value and use against the