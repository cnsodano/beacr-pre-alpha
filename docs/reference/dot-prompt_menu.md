# Thin wrapper around `menu` to allow mocking in tests as mocking `menu` is unsupported\`

Thin wrapper around `menu` to allow mocking in tests as mocking `menu`
is unsupported\`

## Usage

``` r
.prompt_menu(choices, title)
```
