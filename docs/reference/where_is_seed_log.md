# Find where the information needed to verify the seeds acquired by `get_seed` is being written to by default

Find where the information needed to verify the seeds acquired by
`get_seed` is being written to by default

## Usage

``` r
where_is_seed_log()
```
