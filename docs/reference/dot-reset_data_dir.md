# Delete the data directory where seed log and debug log files are stored by default

ONLY intended for development use, e.g. to test starting from a fresh
installation

## Usage

``` r
.reset_data_dir(yes_to_all = FALSE)
```
