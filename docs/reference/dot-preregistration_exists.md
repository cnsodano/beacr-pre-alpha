# Verify if any preregistration information was provided Currently used in get_seed and verify_log

Verify if any preregistration information was provided Currently used in
get_seed and verify_log

## Usage

``` r
.preregistration_exists(preregistration_identifier, preregistration_source)
```
