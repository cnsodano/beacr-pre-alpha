# Check if a value can be represented as an integer without loss

Check if a value can be represented as an integer without loss

## Usage

``` r
.is_integer_like(x)
```

## Arguments

- x:

  A value to check

## Value

`TRUE` if `x` can be converted to an integer without changing its value
