# Convert from NIST 'milliseconds-since-epoch' to a workable datetime via lubridate

Convert from NIST 'milliseconds-since-epoch' to a workable datetime via
lubridate

## Usage

``` r
.ms_to_dt(ms)
```
